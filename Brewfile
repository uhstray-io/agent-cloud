# agent-cloud developer toolchain (LOCAL-DEV-DEPLOYMENT.md Phase 0B).
# Install: `brew bundle` from the repo root. `make local-preflight` verifies.

brew "ansible"
brew "ansible-lint"
brew "bats-core"
brew "gh"
brew "git"
brew "jq"
brew "make"
brew "podman"
brew "podman-compose"
brew "shellcheck"
brew "yamllint"

# Optional — only for root-requiring services (NetBox app tier profile):
# cask "docker"
