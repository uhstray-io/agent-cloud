## 1. Establish read-only diagnosis and source declarations

- [ ] 1.1 Resolve the target inventory and approved credential references from private site-config; verify its instructions/dirty state and preserve them. Encode the approved allowlisted metadata inspection in a reusable operator-side script with existing environment authentication, redacted bounded output and offline tests; keep execution authorization separate. Confirm Semaphore's OpenBao-backed VM key/access mechanism, declared branch binding and exact execution revision; add a bounded reusable access task only if missing. Keep public fixtures synthetic and do not use workstation SSH.
- [ ] 1.2 Make `check-discovery.yml` strictly read-only: remove the GPS setter, replace ignored required-query errors with named failures, use fixed read-only NetBox queries/transactions, and separate secret-bearing reads from visible verdicts. Verify no token mint, scan trigger, configuration write or hidden deploy is reachable from diagnosis.
- [ ] 1.3 Capture a redacted baseline through the read-only path and inspect deployed Orb/Diode/SDK versions, timeout units, source pagination/completeness and available reconciliation correlation. Record operator history separately; classify missing/denied/auth/transport/collection/ingestion faults and unknown downstream stages. Record actual interfaces for the phase-3 adapter before implementing it.
- [ ] 1.4 Add and validate the shared source declaration used by agent rendering and verification: explicit booleans, approved disablement reason, allowed targets, current source cadences, bounded timeouts/deadlines (including credential consumption) and private expected-object selectors. Keep SNMP enabled unless the operator approves a config change; reject empty/invalid declarations and missing prerequisites without mutation.
- [ ] 1.5 Validation gate — exercise spec scenarios **Partial vault access does not establish a root cause**, **Distinct failures remain distinguishable**, **Missing GPS remains unchanged**, **Enabled SNMP has no usable credential**, **Approved disablement remains visible**, **Invalid source declarations refuse execution**, and **Target access uses the platform credential chain** using isolated failure cases plus redacted environment-access evidence; required reads must fail visibly without changing observed data.

## 2. Repair demonstrated faults with safe credential handling

- [ ] 2.1 Extend the discovery credential seed helper to require all fields, validate matching source identity/secret with a read-only source call, read the destination, and refuse different working credentials; remove the fixed Proxmox identity override. Discover authoritative pfSense/SNMP references before requesting operator provisioning, without inventing source values.
- [ ] 2.2 Preserve sibling keys with owned-field updates and KV-version compare-and-set; handle a concurrent update from a fresh read, refuse conflicting owned changes, and skip identical writes. Keep source records unchanged. Cover absent destinations and partial prior attempts.
- [ ] 2.3 Test required agent path access using the agent identity; apply any demonstrated ACL correction via enumerated code-managed policy. Protect AppRole render tasks and runtime files/directories; validate transport before credentials are sent and keep health output visible. Encode only diagnosed collection/ingestion configuration fixes through existing Semaphore deployment tasks.
- [ ] 2.4 If a verified repair requires credential replacement, reuse the existing approved create/verify/select/retire mechanism, retaining recoverable candidate identity/version in OpenBao and proving interrupted retry. If unavailable, implement that bounded missing mechanism before selection. Diagnose the NetBox token provisioner only if it becomes a recovery dependency; do not broaden into Build #1 or mint within verification.
- [ ] 2.5 Validation gate — prove spec scenarios **Invalid candidate leaves working state intact**, **Concurrent sibling update survives recovery**, **Interrupted rotation can resume safely**, and **Credential failure is diagnosable without disclosure** with synthetic secrets, concurrent writes, injected interruptions and captured success/failure output; confirm identical recovery is a no-op.

## 3. Prove collection, reconciliation and expected objects per source

- [ ] 3.1 Add or adapt structured terminal collection outcomes for all four source types with source/run identity, timestamps, complete/partial scope and counts. Missing credentials, exceptions and successful empty collection must produce different outcomes; preserve existing entity identities and merge behavior.
- [ ] 3.2 Implement the smallest version-grounded ingestion observer using the phase-1 interfaces: correlate submission with completed reconciliation, capture failures/timeouts, and refuse unobservable completion. Do not infer acknowledgement from returned entities or successful enqueue; do not add a new telemetry service or fork upstream merely to guess an event shape.
- [ ] 3.3 Implement fresh read-back of each declared expected object projection and its required identity/links, joined to the corresponding cycle evidence. Reject missing, partial, truncated and ambiguous results. Prove static successful cycles without modifying NetBox objects; do not assume global discovery tags identify API worker output.
- [ ] 3.4 Derive per-source freshness from the common declaration, with the design's separate first-recovery consumption allowance and steady-state two-period execution/ingestion budget, five-minute polling, trustworthy timestamps, adequate bounded retention and explicit disabled/unknown states. Record selection time/version and timely agent consumption plus source authentication before judging first recovery; retries retain the original deadline; do not hide a redeploy inside the verifier.
- [ ] 3.5 Validation gate — prove spec scenarios **Collection succeeds but ingestion fails**, **Existing objects do not prove this cycle ran**, **Unchanged successful cycle is healthy**, **Partial or ambiguous results do not pass**, and **Old or unobserved credential consumption cannot pass**. Mutate each gate's input to show it fails independently; a good source must not mask another source's broken pipeline.

## 4. Deliver scheduled observability through existing platform services

- [ ] 4.1 Add the read-only verifier's five-minute Semaphore schedule as code, using the target environment and branch-specific declarations without repointing shared records. Emit bounded sanitized per-source summaries and verifier heartbeats to the existing configured Loki endpoint; validate transport and scoped OpenBao credentials.
- [ ] 4.2 Provision Grafana source-state alert rules and the operator-approved contact route through the existing o11y deployment mechanism. Configure failure/stale/unknown and recovery transitions with quiet unchanged healthy state; verify endpoint and delivery rather than assuming local log collection covers the NetBox VM.
- [ ] 4.3 Add independent verifier-heartbeat absence and telemetry datasource-error alerts, evaluated outside the verifier process. Set the heartbeat deadline to two verifier intervals and verify no-data cannot preserve green status. An unavailable target o11y route remains an explicit blocker.
- [ ] 4.4 Test stale-source timing, notification delivery, recovery transitions, a stopped verifier, and telemetry loss against isolated test sources/routes; record bounded detection times and avoid fault injection into production discovery.
- [ ] 4.5 Validation gate — prove spec scenarios **Healthy pfSense cannot hide stalled Proxmox**, **Quiet success and meaningful recovery notifications**, and **Lost monitor is not a permanent green** through delivered notifications and independent heartbeat expiry evidence, not merely rendered rules.

## 5. Qualify recovery, rollback and documentation

- [ ] 5.1 Update plan 04 and relevant NetBox/operator documentation with evidence provenance, measured outcomes, current cadences, safe seed prerequisites and the read-only gate. Add plan-03 dependency and existing inventory-sync reference; record local Semaphore/D5 reconciliation as separate work and leave SNMPv3/LLDP/runtime conversion/pruning deferred. Do not duplicate env example templates or close unrelated checkboxes.
- [ ] 5.2 Run the affected repository lint and runnable BATS/Python checks, including actual behavioral tests for classification, rendering, read-only guards, credential concurrency/retry and per-source verdicts. Confirm changed tests are included in CI and not vacuous; retain a scenario-to-test/evidence mapping without public private values.
- [ ] 5.3 Validate exact code/config revisions through the dev Semaphore path on a representative Linux target. Run recovery twice and exercise owned-config/verified-credential rollback without deleting NetBox data or losing sibling fields; prove no duplicate objects or credentials. Keep private site-config edits, if later needed, on their own reviewed branch/worktree.
- [ ] 5.4 After normal production promotion/deployment authorization, capture two consecutive scheduled accepted cycles for every enabled source, including expected objects not registered by the verifier. Allow the full SNMP/network periods and credential refresh/ingestion bounds; record any missing operator input or uncovered source as incomplete. Reconfirm the deployed alert route using a clearly labeled isolated test event.
- [ ] 5.5 Validation gate — prove spec scenarios **Recovery repeats and rolls back without data loss** and **Local tests do not close the production gate**, and confirm all preceding source and notification gates pass with exact revision/task references. Record completion only for the approved enabled scope, explicitly reporting any operator-approved disabled source; stop without claiming full recovery if any enabled source or monitoring route remains unproved.
