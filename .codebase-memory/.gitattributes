# Auto-generated by codebase-memory-mcp
# Prevent merge conflicts on compressed artifact
# merge=ours requires a driver named `ours` to exist — it is NOT built in, so
# this line is INERT without `git config merge.ours.driver true`. Run
# `make git-setup` once per clone. Order matters too: `binary` is a macro
# expanding to -diff -merge -text, so it must come BEFORE merge=ours or it
# unsets it.
graph.db.zst binary merge=ours
